# RAP-exempt practitioners (new graduates).
# For these practitioners, the RAP check on bulk-bill EPC patients is set to N/A
# (removed from the audit denominator).
#
# Review yearly as new grads rotate on/off.
# Use Cliniko practitioner IDs when available; the dashboard also falls back to
# a name match (case-insensitive) so either form works.

exempt:
  - name: Mackensey
    # cliniko_id: REPLACE_ME
  - name: Emily
    # cliniko_id: REPLACE_ME

# Last reviewed: 2026-04
