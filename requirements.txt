streamlit>=1.32.0
httpx>=0.27.0
pandas>=2.2.0
plotly>=5.20.0
python-dotenv>=1.0.1
pyyaml>=6.0.1
pytz>=2024.1
anthropic>=0.25.0
Pillow>=10.3.0
# HEIC/HEIF support for iPhone photos — the vision path normalises every
# upload to JPEG before sending to Claude, so this is required any time
# you want to upload .HEIC files straight from an iPhone.
pillow-heif>=0.16.0
